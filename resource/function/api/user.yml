getUser:
  handler: src/api/users.getUser
  events:
    - http:
        path: users
        method: get
        authorizer: customAuthorizer
        cors: true
        request:
          parameters:
            querystrings:
              sub: true
